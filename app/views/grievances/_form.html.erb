<%= form_with(model: grievance, local: true) do |form| %>
  <% if grievance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grievance.errors.count, "error") %> prohibited this grievance from being saved:</h2>

      <ul>
        <% grievance.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :office_id %>
    <%= form.text_field :office_id %>
  </div>

  <div class="field">
    <%= form.label :list_name %>
    <%= form.text_field :list_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
